Pixel Parchment UI Kit — The Bees Knees Games
https://the-bees-knees-games.itch.io/pixel-parchment-ui-kit

STATUS: awaiting download. Drop the zip in this folder, extracted (no .zip is kept
in this tree — see .gitignore).

LICENCE (verified at the source page 2026-07-26, quoted verbatim):
  "A completely free UI set which can be used by any one (including both commercial
   and non commercial projects)."

  Commercial use: yes, explicitly.
  Attribution:    not required (none mentioned).
  Price:          free, "name your own price".

  CAVEAT worth recording: this is an informal permission in the product description,
  not a formal licence (no CC0, no MIT, no licence file referenced on the page). It is
  permissive and unambiguous about commercial use, but if the zip contains a licence
  file, that file wins over this note — read it on arrival and correct this README.

CONTENTS (per the store page):
  PNG files of all art assets
  PSD with menu screens and layers
  Ren'Py source files + Windows/Linux/Mac builds  <- not needed, do not stage
  Instructions for downloading additional assets used

WHY THIS PACK: the prehistoric era's UI. It ships a torn-scroll panel and a dialogue
banner already built, which is a near-exact match for the devolution card frame and
the era-transition couplet container. Of the three era UI kits considered it has both
the cleanest licence and the closest fit, which is why it is first.

Stage only the PNG/PSD art. The Ren'Py project and the platform builds are a game
demo, not assets, and would add tens of MB to a repo that was just cut from 236MB
to 45MB.
